- Se plantea un panel de administracion dentro de componentes en una nueva carpeta llamada useEffect. Componentes: navUser, main, todo esta englobado en un componente llamado gestUser. se crea una carpeta data con db.json, debe tener 5 users {username, email y password}


- crear un .env en el front con la url de la api 
- crear un backend en node que haga un fetching a la api de los pokemons y guarde un endPoint el get de los 20 primeros pokemons y los guarde en la base de datos. Levantar en el puerto 4000
- Cuando añada a favoritos quiero que se almacene a la base de datos